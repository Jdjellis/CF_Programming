# The skill itself needs nothing beyond the Python standard library — the
# deterministic calculator is pure stdlib and runs without an install step.
# The only dependency is the test runner for the calculator's unit tests:
#
#   python3 -m pip install pytest
#   python3 -m pytest
pytest>=7
# Test-only: integration/parity tests against a local Supabase Postgres.
psycopg[binary]>=3
